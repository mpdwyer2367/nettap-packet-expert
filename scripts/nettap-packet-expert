#!/usr/bin/env bash
set -euo pipefail
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ "${1:-}" == update-model ]]; then
  shift
  exec "${script_dir}/nettap-ai" update-models "$@"
fi
exec "${script_dir}/nettap-ai" "$@"
